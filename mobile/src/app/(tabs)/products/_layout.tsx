import { Stack } from 'expo-router';

/** Products stack — index, [id], new screens (Phase 10) */
export default function ProductsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
