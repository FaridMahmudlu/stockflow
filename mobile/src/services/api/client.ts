/**
 * API client with axios instance.
 * Phase 9 will add:
 * - Base URL config
 * - Auth token interceptor (attach access token)
 * - Response interceptor (auto-refresh on 401)
 * - Token storage via expo-secure-store
 *
 * Per SPEC.md Section 6: services/api/client.ts
 */
